import{createState,nextId}from'./state.js';import{createRenderer}from'./renderer.js';import{attachInput}from'./input.js';import{createUI}from'./ui-v16.js';import{rebuildSegments}from'./roads.js';import{hireWorker}from'./workers.js';import{ensureSegmentCarriers,updateCarriers}from'./transport-v16.js';import{updateWorkers}from'./jobs-v16.js';import{dispatchStoredOutputs}from'./logistics-v16.js';
const canvas=document.querySelector('#c'),state=createState(),view={x:innerWidth/2,y:90,zoom:.72};function add(type,x,y,worker=true){const b={id:nextId(state,'building'),type,x,y,input:{},output:{}};state.buildings.push(b);if(worker)hireWorker(state,b);return b}function road(x,y,type='road'){return add(type,x,y,false)}for(let x=8;x<=16;x++)road(x,13);for(const[x,y]of[[8,13],[12,13],[16,13]]){const i=state.buildings.findIndex(b=>b.x===x&&b.y===y&&b.type==='road');if(i>=0)state.buildings.splice(i,1);road(x,y,'flag')}add('store',12,12,false);add('house',11,12,false);add('wood',8,12);add('mill',16,12);for(let i=0;i<55;i++)state.trees.push({id:i+1,x:2+(i*7)%29,y:3+(i*11)%24,alive:true});rebuildSegments(state);ensureSegmentCarriers(state);createRenderer(canvas,state,view);attachInput(canvas,view);createUI(state);let last=performance.now(),dispatch=0;function tick(now){const dt=Math.min(.05,(now-last)/1000);last=now;updateWorkers(state,dt);updateCarriers(state,dt);dispatch+=dt;if(dispatch>.5){dispatch=0;dispatchStoredOutputs(state)}requestAnimationFrame(tick)}requestAnimationFrame(tick);window.settlers={state,view};
