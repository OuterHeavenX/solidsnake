export function createUI(state){const res=document.querySelector('#res'),status=document.querySelector('#status');function render(){if(res)res.innerHTML=`🪵 ${state.inventory.wood||0}　🌲 ${state.inventory.logs||0}　🟫 ${state.inventory.planks||0}　🪨 ${state.inventory.stone||0}　🌾 ${state.inventory.food||0}　👷 ${state.workers.length}`;if(status)status.innerHTML=`Road segments: <b>${state.segments.length}</b><br>Carriers: <b>${state.carriers.length}</b><br>Goods waiting: <b>${state.goods.filter(g=>!g.carrierId).length}</b><br>Workers: <b>${state.workers.length}</b><br>Construction sites: <b>${state.constructionSites.length}</b><br>Builders: <b>${state.builders.length}</b><br>Completed deliveries: <b>${state.stats.delivered}</b>`;requestAnimationFrame(render)}requestAnimationFrame(render);return{render}}
