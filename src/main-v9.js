import{createState,nextId}from'./state.js';import{createRenderer}from'./renderer.js';import{attachInput}from'./input.js';import{createUI}from'./ui.js';import{rebuildSegments}from'./roads.js';import{hireWorker}from'./workers.js';import{ensureSegmentCarriers,updateCarriers}from'./transport-v9.js';import{updateWorkers}from'./jobs.js';import{createConstructionSite}from'./construction.js';import{seedConstructionCargo,updateConstruction}from'./construction-runtime-v9.js';
const canvas=document.querySelector('#c'),state=createState(),view={x:innerWidth/2,y:80,zoom:.72};function add(type,x,y){const b={id:nextId(state,'building'),type,x,y,input:{logs:0}};state.buildings.push(b);hireWorker(state,b);return b}function road(x,y,type='road'){return add(type,x,y)}
for(let x=6;x<=22;x++)road(x,13);for(let y=8;y<=19;y++)road(14,y);for(const[x,y]of[[8,13],[11,13],[14,13],[18,13],[21,13],[14,9],[14,16],[14,19]]){const i=state.buildings.findIndex(b=>b.x===x&&b.y===y&&b.type==='road');if(i>=0)state.buildings.splice(i,1);road(x,y,'flag')}
add('wood',8,12);add('mill',11,12);add('quarry',21,12);const store=add('store',13,16);add('farm',15,19);add('house',12,16);const site=createConstructionSite(state,'house',18,12);
for(let i=0;i<34;i++)state.trees.push({id:i+1,x:3+(i*7)%27,y:4+(i*11)%24,alive:true});for(let i=0;i<22;i++)state.rocks.push({id:i+1,x:4+(i*9)%25,y:5+(i*5)%23,alive:true});for(let i=0;i<20;i++)state.crops.push({id:i+1,x:12+(i%6),y:18+Math.floor(i/6),alive:true,regrow:0});
rebuildSegments(state);ensureSegmentCarriers(state);seedConstructionCargo(state,site,store);createRenderer(canvas,state,view);attachInput(canvas,view);createUI(state);let last=performance.now();function tick(now){const dt=Math.min(.05,(now-last)/1000);last=now;updateWorkers(state,dt);updateCarriers(state,dt);updateConstruction(state,dt);requestAnimationFrame(tick)}requestAnimationFrame(tick);window.modularSettlers={state,site};
